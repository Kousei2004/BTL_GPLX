<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
    </style>

    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/white</item> <!-- Outline color -->
        <item name="hintTextColor">@color/white</item> <!-- Hint color -->
        <item name="android:background">@color/white</item> <!-- Background color -->
        <item name="android:textColor">@color/white</item> <!-- Text color -->
    </style>
</resources>
